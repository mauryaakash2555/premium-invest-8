export const metadata = {
  title: 'Intelligence Archive | BM Wealth',
  description: 'Search through historical market intelligence and headlines',
};

export default function ArchiveLayout({ children }) {
  return children;
}
