/**
 * Plugin entrypoint.
 *
 * Add imports here (each plugin should call registerPlugin on import).
 * Example:
 *   import "@/features/plugins/EmailNotifications";
 */

// No plugins registered by default.







